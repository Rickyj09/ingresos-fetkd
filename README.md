# Ingresos FEDETKD

Sistema web en Flask para registrar ingresos por academias (clubes), eventos, afiliaciones, ascensos, GAL y reportes.

## Requisitos
- Python 3.11+
- MySQL

## Setup rápido
```bash
python -m venv .venv
.\.venv\Scripts\activate
pip install -r requirements.txt